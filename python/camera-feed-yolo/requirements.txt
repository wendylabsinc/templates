fastapi
uvicorn[standard]
opencv-python-headless
ultralytics
# ByteTrack dependency: ultralytics pip-installs lap on first model.track() call,
# which fails in an offline container — bake it at build time.
lap
