fastapi==0.115.6
uvicorn[standard]==0.34.0
# Exact pin: channel/schema APIs changed substantially before 1.0.
foxglove-sdk==0.26.0
# cyclonedds python binding — must match the 0.10.5 C library built in the Dockerfile.
cyclonedds==0.10.5
