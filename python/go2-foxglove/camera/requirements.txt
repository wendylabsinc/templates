# Pinned for reproducible builds.
opencv-python-headless==4.10.0.84
aiortc==1.9.0
aiohttp==3.10.11
httpx==0.28.1
numpy==1.26.4
# unitree_webrtc_connect is not reliably versioned on PyPI — pin via your index
# or a git ref if your environment provides one.
unitree_webrtc_connect
