# WebRTC audiohub megaphone stack (same as the camera service). No CycloneDDS —
# the DDS rt/audioreceiver path was unreliable; the megaphone file API plays.
fastapi==0.115.6
uvicorn[standard]==0.34.0
aiortc==1.9.0
aiohttp==3.10.11
numpy==1.26.4
# unitree_webrtc_connect is not reliably versioned on PyPI — pin via the index
# or a git ref if your environment provides one.
unitree_webrtc_connect
