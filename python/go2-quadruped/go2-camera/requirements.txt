fastapi
uvicorn[standard]
opencv-python-headless
aiortc
aiohttp
unitree_webrtc_connect
numpy
# Pin to 0.10.x — only matches a CycloneDDS C library at 0.10.x. The
# Dockerfile builds 0.10.5 from source; bumping this without rebuilding
# the C lib will fail at import time with a header / ABI mismatch.
cyclonedds<0.11
