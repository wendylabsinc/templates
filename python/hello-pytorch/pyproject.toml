[project]
name = "hello-pytorch"
version = "0.1.0"
description = "PyTorch GPU/CPU availability checker"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
