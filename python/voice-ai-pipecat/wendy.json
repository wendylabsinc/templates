{
    "appId": "{{.APP_ID}}",
    "version": "0.1.0",
    "entitlements": [
        {
            "type": "network",
            "mode": "host"
        },
        {
            "type": "audio"
        },
        {
            "type": "persist",
            "name": "{{.APP_ID}}-models",
            "path": "/models"
        }
    ],
    "readiness": {
        "tcpSocket": { "port": {{.PORT}} },
        "timeoutSeconds": 300
    },
    "hooks": {
        "postStart": {
            "cli": "wendy utils open-browser https://${WENDY_HOSTNAME}:{{.PORT}}"
        }
    }
}
