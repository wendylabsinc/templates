pipecat-ai[websocket,google,whisper,piper]<1.0
# The Swift template uses the generic Swift runtime image and runs Whisper
# on CPU. Keep CTranslate2 on the CUDA 12 + cuDNN 8 compatible line so the
# dependency contract stays aligned with the Jetson Python template.
ctranslate2==4.4.0
faster-whisper>=1.0,<2.0
piper-tts
fastapi
uvicorn[standard]
websockets
python-dotenv
nltk
